import { Button } from 'antd';

import { MainLayout } from '@/components/layouts';

export default function HomePage() {
    return (
        <MainLayout>
            <Button>abc</Button>
        </MainLayout>
    );
}
